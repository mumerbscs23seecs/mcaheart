---
import '../styles/global.css';
import FontLinks from './FontLinks.astro';
import ThemeInit from './ThemeInit.astro';
import LogoMark from './LogoMark.astro';
import { site } from '../data/site';

interface Props {
  title: string;
  description?: string;
}
const { title, description = 'MCA Heart Research Lab members area.' } = Astro.props;
const pageTitle = `${title} | ${site.name}`;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <ThemeInit />
    <title>{pageTitle}</title>
    <meta name="description" content={description} />
    <meta name="robots" content="noindex" />
    <link rel="icon" href="/favicon.png" type="image/png" />
    <meta name="theme-color" content="#ffffff" media="(prefers-color-scheme: light)" />
    <meta name="theme-color" content="#141412" media="(prefers-color-scheme: dark)" />
    <FontLinks />
  </head>

  <body class="authbody">
    <main class="authpage">
      <div class="authpage__inner">
        <a class="authpage__brand" href="/" aria-label="MCA Heart - home">
          <LogoMark size="1.9em" />
          <span class="authpage__word"><b>MCA</b> HEART</span>
        </a>

        <slot />

        <p class="authpage__foot"><a href="/">&larr; Back to main site</a></p>
      </div>
    </main>

    <style is:global>
      .authbody {
        min-height: 100dvh;
        background: var(--bg);
      }
      .authpage {
        min-height: 100dvh;
        display: grid;
        place-items: center;
        padding: var(--space-l) var(--gutter);
      }
      .authpage__inner {
        width: 100%;
        max-width: 380px;
        display: grid;
        gap: var(--space-m);
      }

      .authpage__brand {
        justify-self: center;
        display: inline-flex;
        align-items: center;
        gap: 0.5em;
        font-family: var(--font-display);
        font-weight: 800;
        font-size: 1rem;
        text-transform: uppercase;
        letter-spacing: -0.01em;
        color: var(--text-strong);
      }
      .authpage__brand b { color: var(--blue); font-weight: 800; }
      /* --- The card --- */
      .auth-card {
        display: grid;
        gap: var(--space-s);
        padding: clamp(1.6rem, 1.2rem + 1.6vw, 2.4rem);
        background: var(--surface);
        border: 1px solid var(--ink);
        border-top: 3px solid var(--blue);
        border-radius: 0;
        box-shadow: none;
      }
      .auth-card__title {
        font-family: var(--font-display);
        font-weight: 800;
        font-size: 1.7rem;
        line-height: 1.05;
        letter-spacing: -0.025em;
        color: var(--text-strong);
      }
      .auth-card__sub {
        margin-top: -0.55rem;
        font-size: 0.86rem;
        color: var(--text-muted);
      }

      .auth-field { display: grid; gap: 0.4rem; }
      .auth-field label {
        font-size: 0.8rem;
        font-weight: 600;
        color: var(--text);
      }
      .auth-field input {
        width: 100%;
        padding: 0.7rem 0.85rem;
        background: var(--surface);
        border: 1px solid var(--border);
        border-radius: 0;
        font-size: 0.95rem;
        color: var(--text-strong);
      }
      .auth-field input:focus {
        outline: none;
        border-color: var(--blue);
        box-shadow: 0 0 0 3px var(--accent-wash-strong);
      }

      .auth-card .btn {
        width: 100%;
        justify-content: center;
        margin-top: 0.3rem;
        padding-block: 0.8em;
      }

      .auth-card__link {
        justify-self: start;
        font-size: 0.85rem;
        color: var(--blue);
      }
      .auth-card__link:hover { text-decoration: underline; }

      .auth-card__lead { font-size: 0.88rem; color: var(--text-muted); line-height: 1.6; }
      .auth-card__lead strong { color: var(--text-strong); }

      .auth-alert {
        padding: 0.65rem 0.85rem;
        border-radius: 0;
        font-size: 0.85rem;
        font-weight: 600;
        color: var(--red);
        background: var(--red-wash);
        border: 1px solid var(--red-border);
      }
      .auth-ok-box {
        padding: 0.85rem 1rem;
        border-radius: 0;
        font-size: 0.86rem;
        line-height: 1.6;
        color: var(--ok);
        background: var(--ok-bg);
        border: 1px solid var(--ok-border);
      }
      .auth-linkbox {
        padding: 0.75rem 0.9rem;
        background: var(--bg-tint);
        border: 1px solid var(--border-soft);
        border-radius: 0;
        font-size: 0.78rem;
        line-height: 1.55;
        overflow-wrap: anywhere;
      }
      .auth-linkbox a { color: var(--blue); font-family: var(--font-mono); }

      /* --- Below-card bits --- */
      .authpage__note {
        justify-self: center;
        text-align: center;
        font-size: 0.8rem;
        color: var(--text-muted);
      }
      .authpage__note a { color: var(--blue); }

      .authpage__demo {
        border: 1px solid var(--border-soft);
        border-radius: 0;
        background: var(--surface);
        font-size: 0.8rem;
      }
      .authpage__demo summary {
        padding: 0.6rem 0.9rem;
        cursor: pointer;
        color: var(--text-muted);
        font-weight: 600;
        list-style: none;
      }
      .authpage__demo summary::-webkit-details-marker { display: none; }
      .authpage__demo summary::before { content: '▸ '; }
      .authpage__demo[open] summary::before { content: '▾ '; }
      .authpage__demo table { width: 100%; border-collapse: collapse; }
      .authpage__demo td {
        padding: 0.35rem 0.9rem 0.35rem 0;
        border-top: 1px solid var(--border-soft);
        font-family: var(--font-mono);
        font-size: 0.76rem;
        color: var(--text);
      }
      .authpage__demo td:first-child { padding-left: 0.9rem; color: var(--text-muted); font-family: var(--font-sans); }

      .authpage__foot {
        justify-self: center;
        font-size: 0.8rem;
      }
      .authpage__foot a { color: var(--text-muted); }
      .authpage__foot a:hover { color: var(--blue); }
    </style>
  </body>
</html>
