---
// "My Papers" was folded into the phase tabs (each scoped to the member's own
// papers). Keep the URL alive for old links.
export const prerender = false;
return Astro.redirect('/pipeline/conferences', 302);
---
